import type { SketchfabSearchResult } from "@/types";

export function mapSketchfabResult(input: SketchfabSearchResult): SketchfabSearchResult {
  return input;
}

// TODO: Normalize real Sketchfab API responses into the stable local shape.
