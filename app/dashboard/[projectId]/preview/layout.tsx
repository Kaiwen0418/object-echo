import { MuseumPageLayout } from "@/components/layout/MuseumPageLayout";

export default function ProjectPreviewLayout({ children }: { children: React.ReactNode }) {
  return <MuseumPageLayout>{children}</MuseumPageLayout>;
}
